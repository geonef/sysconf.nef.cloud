#
# AUTOMATICALLY GENERATED BY nef-cloud
#

geonef_zig:
  install:
    cdn_root_url: "http://${NEF_SERVICE_CDN_DOMAIN}/"

doctrine_mongodb:
  connections:
    localnode:
      server: "mongodb://$(nef_mongodb_host):27017"
      # options:
      #   # from MongoDBBundle/DependencyInjection/Configuration.php
      #   connect: true
      #   persist: true
      #   timeout: 10
      #   replicaSet: ?
      #   slaveOkay: ?
      #   username: lala
      #   password: lala

  document_managers:
    default:
      connection: localnode
      database: "$(nef_mongodb_dbname)"
    localnode:
      connection: localnode
      database: "$(nef_mongodb_dbname)"

swiftmailer:
  transport: smtp
  host: $(nef_info_service_hostname mx)
  encryption: null
  auth_mode: null
